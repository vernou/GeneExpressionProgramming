# Copyright (c) 2013 The Chromium Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

# GNU Makefile based on shared rules provided by the Native Client SDK.
# See README.Makefiles for more details.

VALID_TOOLCHAINS := newlib glibc pnacl win

NACL_SDK_ROOT ?= $(abspath $(CURDIR)/../..)
include $(NACL_SDK_ROOT)/tools/common.mk

TARGET = gep
LIBS = $(DEPS) ppapi_cpp ppapi pthread

CFLAGS = -std=c++11 -O2 -Wall 
# -std=c++11 -stdlib=libc++ -std=gnu++11
SOURCES = 	src/gep_nacl.cc \
			src/Message.cpp \
			../src/GEP_PARSE/ParserCSV.cpp \
            ../src/GEP/Display.cpp \
            ../src/GEP/Element.cpp \
            ../src/GEP/Expression.cpp \
            ../src/GEP/Factory.cpp \
            ../src/GEP/GeneticExpression.cpp \
            ../src/GEP/Node.cpp \
            ../src/GEP/Operator.cpp \
            ../src/GEP/Population.cpp \
            ../src/GEP/Result.cpp \
            ../src/GEP/Setting.cpp \
            ../src/GEP/TestData.cpp 

# Build rules generated by macros from common.mk:

$(foreach src,$(SOURCES),$(eval $(call COMPILE_RULE,$(src),$(CFLAGS))))

ifeq ($(CONFIG),Release)
$(eval $(call LINK_RULE,$(TARGET)_unstripped,$(SOURCES),$(LIBS),$(DEPS)))
$(eval $(call STRIP_RULE,$(TARGET),$(TARGET)_unstripped))
else
$(eval $(call LINK_RULE,$(TARGET),$(SOURCES),$(LIBS),$(DEPS)))
endif

$(eval $(call NMF_RULE,$(TARGET),))
